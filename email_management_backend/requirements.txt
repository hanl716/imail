fastapi
uvicorn[standard]
SQLAlchemy
psycopg2-binary
python-dotenv
alembic
python-jose[cryptography]
passlib[bcrypt]
python-multipart
email-validator
celery
redis
gevent # For Celery worker with -P gevent
cerebras-cloud-sdk
slowapi
pytest
pytest-asyncio
httpx # Often a dependency of SDKs, but good to have for general testing
# respx # unittest.mock.patch is used in the example, so respx might not be immediately needed
# pytest-httpx # Alternative to respx
